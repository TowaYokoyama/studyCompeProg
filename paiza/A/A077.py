"""
ある海域には、海底火山が N 箇所あります。海底火山が噴火すると溶岩が冷えて固まり、火山がある場所を中心とする半径 R の円を描くように島ができます。複数の海底火山が同時に噴火したとき、お互いの距離が 2R 以下の海底火山によってできる島同士は互いに結合して、1 つの島になります。

すべての海底火山が同時に噴火したときにできる島の数を求めてください。

海底火山の位置は (x, y) 座標で与えられます。与えられる島の x 座標および、y 座標は 0 以上の整数で与えられますが、噴火でできる島の範囲は、x 座標および、y 座標の負の領域に及ぶ場合があることに注意してください。

入力例 1 の場合、以下のように火山があります。



火山が噴火すると、それぞれの火山を中心とする半径 1 の島ができます。そのとき、以下の図のように、接するか、重なった島は結合し、1 つの島となります。2 つの島が接しているときも結合していると判定することに注意してください。


評価ポイント
10個のテストケースを入力し、正答数と解答の提出までに要した時間を測定し得点が決まります。
※提出いただいたコードは複数回実行され、一度の実行では1つのテストケースのみ入力
※制限時間を超えるとテストケースが通っても失格(0点)となります。
得点の計算方法：正解数得点(50点) ＋ 正解率×解答時間得点(2時間以内で50点、4時間以内で25点、6時間で0点と線形に点数が落ちます)
10個のテストケースで正しい出力がされるか評価 (50点)
解答の提出までに要した時間による評価 (50点)
入力される値
入力は次のフォーマットで与えられます。
N R
x_1 y_1
x_2 y_2
...
x_N y_N

・1 行目にはそれぞれ海底火山の数、火山が噴火したときにできる島の半径を表す整数 N, R が半角スペース区切りで与えられます。
・続く N 行のうちの i 行目 (1 ≦ i ≦ N) には、i 番目の海底火山の座標を表す整数 x_i, y_i がこの順で半角スペース区切りで与えられます。
・入力は合計で N + 1 行となり、入力値最終行の末尾に改行が 1 つ入ります。

文字列は標準入力から渡されます。標準入力からの値取得方法はこちらをご確認ください
期待する出力
海底火山が同時に噴火した際にできる島の数を整数で出力してください。

末尾に改行を入れ、余計な文字、空行を含んではいけません。
条件
すべてのテストケースにおいて、以下の条件をみたします。
・1 ≦ N, R ≦ 100
・0 ≦ x_i, y_i < 100 (1 ≦ i ≦ N)
・各 j, k (1 ≦ j, k ≦ N) について j ≠ k のとき (x_j, y_j) ≠ (x_k, y_k)

言語別実行時間制限の詳細は こちら をご確認ください。
入力例1
5 1
2 2
1 3
6 1
6 3
7 4
出力例1
2
入力例2
3 3
1 2
4 2
4 5
出力例2
1
入力例3
10 10
66 7
59 87
79 59
76 12
27 45
73 87
13 49
77 81
21 95
88 75
出力例3
4
"""
N,R = map(int,input().split())
coords = [tuple(map(int,input().split())) for _ in range(N)]
# Union-Find: 各ノードの親を管理する配列
# 最初は全員が自分自身の親（= 独立した島）
parent = list(range(N))

def find(x):
    while parent[x] != x:
        parent[x] = parent[parent[x]]
        x = parent[x]
        
    return x 

def union(x,y):
    parent[find(x)] = find(y)
    
for i in range(N):
    for j in range(i+1,N):
        dx = coords[i][0] - coords[j][0]
        dy = coords[i][1] - coords[j][1]
        # 距離 <= 2R なら島が結合する
        # √(dx²+dy²) <= 2R の両辺を二乗 → dx²+dy² <= (2R)²
        if dx*dx + dy*dy <= (2*R)**2:
            union(i,j)
#ねの種類数=島の数
print(len(set(find(i) for i in range(N))))